# Every check on, including style: comment-shape drift fails the gate
# instead of accumulating. Configured here rather than as a CLI flag so
# the generated-code directory can subtract the one check regeneration
# would keep re-breaking (see internal/store/sqlitedb/staticcheck.conf);
# a -checks flag on the command line would override both files.
checks = ["all"]
