# Build output
/runpool
/capsule-supervisor
/dist/
/coverage.out
/release-qualification.json
/platform-facts.json
/*-contract.log

# Local configuration and state
.env
*.db
*.db-shm
*.db-wal
# Except the released-schema fixture, which is test input rather than
# somebody's state: it is what a database v1.0.0 wrote, kept so the first
# migration meets one it did not write before an operator's does.
!internal/store/testdata/*.db

# OS noise
.DS_Store
._*
.idea/
.vscode/
