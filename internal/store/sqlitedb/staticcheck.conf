# Generated by sqlc (see sqlc.yaml); regeneration would erase any
# package comment we added by hand, so the package-comment check is off
# here and only here. Every other check still applies — generated code
# that trips a correctness check is a real finding against the queries.
checks = ["inherit", "-ST1000"]
